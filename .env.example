#
# AuroraPad local environment template
# Copy values into a non-tracked `.env.local` when needed.
#
# Never commit real tokens, private keys, passwords, or certificates.
#
# Example only:
# VERCEL_OIDC_TOKEN=""
